{
  "name": "nova-voiceai-platform",
  "version": "2.0.0",
  "private": true,
  "description": "NOVA — Voice-first AI platform. Run everything with one command.",
  "scripts": {
    "dev": "npx -y concurrently -n backend,frontend -c blue,green \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "build:frontend": "npm run build --prefix frontend",
    "build:all": "node scripts/build.js",
    "start": "node backend/server.js",
    "test": "node --test backend/tests/*.test.js",
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "lint": "npm run lint --prefix frontend"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": ["voice-ai", "ai-workspace", "nova"],
  "license": "MIT"
}
